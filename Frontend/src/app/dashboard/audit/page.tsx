import { PageHeader } from '@/components/PageHeader';

export default function AuditPage() {
  return (
    <PageHeader
      title="Audit Logs"
      subtitle="Filterable privileged-action history."
    />
  );
}
