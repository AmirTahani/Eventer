import { PageHeader } from '@/components/PageHeader';

export default function DjsPage() {
  return (
    <PageHeader
      title="DJs"
      subtitle="Shared DJ pool — create and reuse profiles across events."
    />
  );
}
