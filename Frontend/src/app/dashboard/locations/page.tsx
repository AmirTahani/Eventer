import { PageHeader } from '@/components/PageHeader';

export default function LocationsPage() {
  return (
    <PageHeader
      title="Locations"
      subtitle="Shared venue pool. Addresses stay hidden from attendees until release."
    />
  );
}
