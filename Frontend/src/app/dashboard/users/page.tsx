import { PageHeader } from '@/components/PageHeader';

export default function UsersAdminPage() {
  return (
    <PageHeader
      title="Users"
      subtitle="Admin-only: approve, suspend, and assign roles."
    />
  );
}
